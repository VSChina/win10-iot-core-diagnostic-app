<Page
    x:Class="Win10IoTCoreDiagnosticApp.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Win10IoTCoreDiagnosticApp"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
            <TextBox x:Name="HelloMessage" Text="Click button to send Diagnostic messages" Margin="10" IsReadOnly="True" />
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                <Button x:Name="StartBtn" Content="Start"  Margin="10" HorizontalAlignment="Center" Click="StartD2CMessageClick"/>
                <Button x:Name="StopBtn" Content="Stop"  Margin="10" HorizontalAlignment="Center" Click="StopD2CMessageClick"/>
            </StackPanel>
            
            <TextBlock Height="300" x:Name="TipBox"></TextBlock>
        </StackPanel>
    </Grid>
</Page>
